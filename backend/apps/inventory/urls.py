from django.urls import path
from . import views

urlpatterns = [
    # Aquí van las rutas de tu app de productos
    # Ejemplo:
    # path('', views.ProductListView.as_view(), name='product-list'),
]
